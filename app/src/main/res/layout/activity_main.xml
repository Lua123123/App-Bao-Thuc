<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="text"
        android:textColor="#000000"
        android:textSize="30sp"
        android:textStyle="bold" />

    <TimePicker
        android:id="@+id/timePicker"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:amPmBackgroundColor="@color/black"
        android:backgroundTint="@color/black"
        android:headerBackground="@drawable/bg_bottom_nav"
        android:numbersBackgroundColor="@android:color/holo_orange_dark" />

    <Button
        android:id="@+id/btnHenGio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_btn"
        android:text="HẸN GIỜ"
        android:textSize="17sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btnDung"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_btn"
        android:text="DỪNG LẠI"
        android:textSize="17sp"
        android:textStyle="bold" />
</LinearLayout>